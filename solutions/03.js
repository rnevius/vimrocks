// Substitute: :%s/Eq/.eq
// Macro: 0%"adT 2X%aa, 2j
// Visually select the remaining lines and run the macro.

assert.equal(actual, expected);

assert.equal(my.string, "abc");

assert.equal(my.longString, "A long string");

assert.equal(my.array, ["an", "array"]);

assert.equal(my.object, { an: "object" });

assert.equal(my.second_object, { and: "also",
              multiline: "objects"
            });

