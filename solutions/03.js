// let @s = "0:s/tE/t.e%"adT 2X%aa, 2j"
// Set up a mapping to do this automatically:
// nnoremap <Leader>s 0:s/tE/t.e<CR>%"adT<Space>2X%a<C-r>a,<Space><Esc>

assert.equals(actual, expected);

assert.equals(my.string, "abc");

assert.equals(my.longString, "A long string");

assert.equals(my.array, ["an", "array"]);

assert.equals(my.object, { an: "object" });

assert.equals(my.second_object, { and: "also",
               multiline: "objects"
             });

